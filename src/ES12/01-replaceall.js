//el metodo replace reemplaza el texto o palabras que se pongan en un parrfo por qejemplo con otras que se pongna
const stirng = 'javascript un lorem'


//.replace(palabraQueseQuiereRemp, PalabraRmp)
const replace = stirng.replace('javascript', 'nosees')

console.log(replace);